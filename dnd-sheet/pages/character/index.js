import Head from 'next/head'
import Link from 'next/link'
import styles from '../../styles/Home.module.css'
import { useEffect, useState } from 'react'
import socketIOClient from 'socket.io-client'
import { rollDice } from '../util/roll'

const Character = () => {
    const [socket, setSocket] = useState(null);
    const [messages, setMessages] = useState([]);
    const [name, setName] = useState(''); // not sure wanna use input since im not actually submiting to a backend

    useEffect(() => {
        console.log("yoo")
        const socket = socketIOClient('localhost:3001');
        setSocket(socket)
        socket.on('connect', (data) => {
            console.log('client connected successfully!')
        })
        socket.on('message',(msg) => {
            setMessages( msgs => [...msgs, msg])
        })
    },[setSocket]);

    const handleRoll = () => {
        const randomNumber = Math.floor(Math.random() * 6) + 1;
        socket.emit('message', `${name} rolled a ${randomNumber}!`)
    }

    const handleNameChange = (e) => {
        setName(e.target.value)
    }

    return (
        <div className="container mx-auto mt-4">
            <Head>
                <title>DnD Character Sheet</title>
                <meta name="Customized dnd character sheet" content="Generated by create next app" />
                <link rel="icon" href="/favicon.ico" />
            </Head>

            <div id="actionBar" className="flex flex-row flex-nowrap justify-start">
                <button class="text-white bg-blue-700 hover:bg-blue-800 rounded-lg px-5 py-2.5 mr-2 mb-2">Save Changes</button>
                <button class="text-white bg-blue-700 hover:bg-blue-800 rounded-lg px-5 py-2.5 mr-2 mb-2">Load</button>
                <button class="text-white bg-blue-700 hover:bg-blue-800 rounded-lg px-5 py-2.5 mr-2 mb-2">Login</button>
                <button class="text-white bg-blue-700 hover:bg-blue-800 rounded-lg px-5 py-2.5 mr-2 mb-2"><Link href="/">Home</Link></button>
            </div>

            <div class="mb-6">
                <label for="default-input" class="block mb-2 text-sm font-medium text-gray-900 dark:text-gray-300">Character Name</label>
                <input type="text" id="default-input" class="bg-gray-50 border border-gray-300 text-gray-900 text-lg font-bold rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500" placeholder="Character Name" />
            </div>
        </div>


        // <div className={styles.container}>
        //     <Head>
        //         <title>DnD Character Sheet</title>
        //         <meta name="Customized dnd character sheet" content="Generated by create next app" />
        //         <link rel="icon" href="/favicon.ico" />
        //     </Head>

        //     <main className={styles.charMain}>
        //         <div className={styles.wrapper}>
        //             <div className={`${styles.box} ${styles.charName}`}>
        //                 character name
        //                 <label>Name</label>
        //                 <input type="text" id="name" name="name" onChange={handleNameChange} />
        //             </div>
        //             <div className={`${styles.box} ${styles.charSaves}`}>character health/AC</div>
        //             <div className={`${styles.box} ${styles.charHealth}`}>character saves</div>
        //             <div className={`${styles.box} ${styles.chatBox}`}>
        //                 chat box
        //                 <ul>
        //                     {messages.map((msg) => {
        //                         return(
        //                             <li>{msg}</li>
        //                         )
        //                     })}
        //                 </ul>
        //             </div>
        //             <div className={`${styles.box} ${styles.charActions}`}>
        //                 character actions
        //                 <button onClick={handleRoll}>click to roll</button>
        //             </div>
        //         </div>
        //     </main>

        //     <footer>
        //         <Link href="/">
        //             <a>go home</a>
        //         </Link>
        //     </footer>
        // </div>
    );
}

export default Character;